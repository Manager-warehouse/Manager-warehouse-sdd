openapi: 3.0.3
info:
  title: Driver Mobile POD API
  version: 1.0.0
  description: Contracts for driver mobile POD upload, OTP confirmation, failed delivery, trip completion, returned-goods warehouse processing, and admin OTP reset in Sprint 1.
paths:
  /api/v1/trips/driver:
    get:
      summary: List assigned trips for driver mobile
      operationId: listDriverTrips
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Assigned driver trips across DELIVERY and TRANSFER trip types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TripDriverViewResponse"
        "403":
          description: Authenticated user is not a Driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{id}:
    get:
      summary: Get assigned trip detail for driver mobile
      operationId: getDriverTrip
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Assigned trip detail with Delivery Orders and current attempt data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TripDriverViewResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Trip not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{tripId}/delivery-orders/{doId}/pod-evidence:
    post:
      summary: Upload POD evidence for a Delivery Order in the assigned trip
      operationId: uploadPodEvidence
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: tripId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - goodsImage
                - signDocumentImage
              properties:
                goodsImage:
                  type: string
                  format: binary
                signDocumentImage:
                  type: string
                  format: binary
                notes:
                  type: string
                  maxLength: 1000
      responses:
        "200":
          description: POD evidence stored on the current delivery attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryAttemptResponse"
        "400":
          description: Missing or invalid POD file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Current delivery attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Delivery attempt is no longer current or already finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Persistent local POD storage is unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/pod-evidence/{evidenceType}:
    get:
      summary: Stream one POD image from persistent local VPS storage
      operationId: getPodEvidenceImage
      tags:
        - Delivery Orders
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: evidenceType
          in: path
          required: true
          schema:
            type: string
            enum: [GOODS, SIGNED_DOCUMENT]
      responses:
        "200":
          description: Authorized inline POD image response
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        "403":
          description: User cannot view the Delivery Order detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Successful delivery attempt, POD metadata, or local file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{tripId}/delivery-orders/{doId}/delivery-otp:
    post:
      summary: Generate or resend OTP for the current delivery attempt
      operationId: requestDeliveryOtp
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: tripId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeliveryOtpRequest"
      responses:
        "200":
          description: OTP generated or resent for the current delivery attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryOtpResponse"
        "400":
          description: POD evidence missing or payload invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Delivery attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: OTP is still active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Dealer email missing or OTP is locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: OTP email delivery failed; the same OTP row is SEND_FAILED and immediately retryable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{tripId}/delivery-orders/{doId}/confirm-delivery:
    put:
      summary: Confirm full Delivery Order delivery with dealer OTP
      operationId: confirmDelivery
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: tripId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfirmDeliveryRequest"
      responses:
        "200":
          description: Delivery confirmed and Delivery Order completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryAttemptResponse"
        "400":
          description: Invalid OTP, expired OTP, missing POD, or malformed payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Delivery attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Delivery already finalized or concurrent inventory/financial conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Partial delivery attempted or In-Transit stock missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "423":
          description: OTP locked and requires admin reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/split-delivery-plans/{planId}/dealer-arrival:
    put:
      summary: Lead driver confirms the whole split convoy arrived at the dealer
      operationId: confirmSplitDealerArrival
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SplitPlanId"
      responses:
        "200":
          description: Dealer arrival recorded for the whole split convoy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SplitLegMilestoneResponse"
        "403":
          description: Authenticated driver is not the split lead driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Split plan is no longer in transit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/split-delivery-plans/{planId}/handover:
    put:
      summary: Lead driver confirms whole split Delivery Order handover
      operationId: confirmSplitHandover
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SplitPlanId"
      responses:
        "200":
          description: Handover recorded for the whole split Delivery Order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SplitLegMilestoneResponse"
        "403":
          description: Authenticated driver is not the split lead driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Lead driver has not confirmed whole-convoy dealer arrival
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/split-delivery-plans/{planId}/fail-delivery:
    put:
      summary: Lead driver reports split delivery failure and returns the whole Delivery Order
      operationId: failSplitDelivery
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SplitPlanId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SplitLegFailureRequest"
      responses:
        "200":
          description: Whole split Delivery Order moved to return handling
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SplitLegMilestoneResponse"
        "403":
          description: Authenticated driver is not the split lead driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Delivery Order is already finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/split-delivery-plans/{planId}/complete:
    put:
      summary: Lead driver confirms the whole split convoy returned to the source warehouse
      operationId: completeSplitDeliveryConvoy
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SplitPlanId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TripCompleteRequest"
      responses:
        "200":
          description: Split convoy completed and all linked drivers/vehicles released
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SplitLegMilestoneResponse"
        "403":
          description: Authenticated driver is not the split lead driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Split Delivery Order is not ready for convoy return completion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{tripId}/delivery-orders/{doId}/fail-delivery:
    put:
      summary: Record dealer refusal or delivery failure
      operationId: failDelivery
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: tripId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FailDeliveryRequest"
      responses:
        "200":
          description: Delivery attempt failed and Delivery Order moved to RETURNED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryAttemptResponse"
        "400":
          description: Invalid request payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Delivery attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Delivery already finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/trips/{tripId}/complete:
    put:
      summary: Confirm vehicle return and complete the assigned trip
      operationId: completeDriverTrip
      tags:
        - Driver Trips
      security:
        - bearerAuth: []
      parameters:
        - name: tripId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TripCompleteRequest"
      responses:
        "200":
          description: Trip completed and driver/vehicle released
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TripDriverViewResponse"
        "400":
          description: Invalid request payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Driver is not assigned to the trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Trip not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Trip is not ready to complete
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/admin/delivery-orders/{doId}/delivery-otp/reset:
    post:
      summary: Reset a locked OTP for the current delivery attempt
      operationId: resetDeliveryOtp
      tags:
        - Admin Delivery
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetDeliveryOtpRequest"
      responses:
        "200":
          description: Locked OTP reset for the current delivery attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryOtpResponse"
        "400":
          description: Invalid request payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Current delivery attempt or OTP row not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "423":
          description: OTP reset requested for a row that is not in a resettable locked state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods:
    get:
      summary: Get returned-goods flow state
      operationId: getReturnedGoodsFlow
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Current returned-goods flow state for the Delivery Order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Returned-goods flow has not been submitted yet or Delivery Order state is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods/receive:
    put:
      summary: Storekeeper confirms returned goods arrived back at the warehouse
      operationId: confirmReturnedGoodsReceived
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnedGoodsReceiveRequest"
      responses:
        "200":
          description: Returned goods arrival confirmed; staff count/QC is now pending while Delivery Order remains RETURNED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Delivery Order is not RETURNED or returned-goods flow is not in a receivable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods/count-qc:
    put:
      summary: Submit or resubmit returned-goods count and quality inspection
      operationId: submitReturnedGoodsCountQc
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnedGoodsCountQcRequest"
      responses:
        "200":
          description: Returned goods actual/pass/fail count/QC submitted while Delivery Order remains RETURNED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Delivery Order state, quantity, or quality result validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods/approval:
    put:
      summary: Storekeeper accepts or rejects returned-goods count and quality
      operationId: decideReturnedGoodsQc
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnedGoodsApprovalRequest"
      responses:
        "200":
          description: Returned goods quantity and quality accepted, or rejected back to staff rework, while Delivery Order remains RETURNED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Count/QC result is incomplete, rejection reason is missing, or decision cannot be applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods/putaway-plan:
    put:
      summary: Storekeeper creates returned-goods putaway plan
      operationId: planReturnedGoodsPutaway
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnedGoodsPutawayPlanRequest"
      responses:
        "200":
          description: Returned goods putaway plan created while Delivery Order remains RETURNED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Storekeeper QC acceptance or destination-location validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/delivery-orders/{doId}/returned-goods/putaway-complete:
    put:
      summary: Warehouse staff confirms returned-goods putaway completion
      operationId: completeReturnedGoodsPutaway
      tags:
        - Returned Delivery Goods
      security:
        - bearerAuth: []
      parameters:
        - name: doId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnedGoodsPutawayCompleteRequest"
      responses:
        "200":
          description: Returned goods put away; inventory moved from IN_TRANSIT and Delivery Order moved to DELIVERY_FAILED
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnedGoodsFlowResponse"
        "422":
          description: Putaway plan is missing or completion does not match the plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    SplitPlanId:
      name: planId
      in: path
      required: true
      schema:
        type: integer
        format: int64
  schemas:
    DeliveryOtpRequest:
      type: object
      properties:
        resend:
          type: boolean
          default: false
    ConfirmDeliveryRequest:
      type: object
      required:
        - otp
      properties:
        otp:
          type: string
          pattern: "^[0-9]{6}$"
        notes:
          type: string
          maxLength: 1000
    FailDeliveryRequest:
      type: object
      required:
        - failureReason
      properties:
        failureReason:
          type: string
          minLength: 1
          maxLength: 1000
        notes:
          type: string
          maxLength: 1000
    ResetDeliveryOtpRequest:
      type: object
      required:
        - resetReason
      properties:
        resetReason:
          type: string
          minLength: 1
          maxLength: 1000
        notes:
          type: string
          maxLength: 1000
    TripCompleteRequest:
      type: object
      properties:
        returnedAt:
          type: string
          format: date-time
        notes:
          type: string
          maxLength: 1000
    SplitLegFailureRequest:
      type: object
      required:
        - failureReason
      properties:
        failureReason:
          type: string
          minLength: 1
          maxLength: 1000
    SplitLegMilestoneResponse:
      type: object
      properties:
        splitPlanId:
          type: integer
          format: int64
        legId:
          type: integer
          format: int64
          nullable: true
          description: Optional lead leg identifier when the backend exposes one; lead-only endpoints may omit it.
        status:
          type: string
          enum: [IN_TRANSIT, COMPLETED, RETURNED]
        dealerArrivedAt:
          type: string
          format: date-time
          nullable: true
        handoverConfirmedAt:
          type: string
          format: date-time
          nullable: true
        failureReportedAt:
          type: string
          format: date-time
          nullable: true
        allLegsArrived:
          type: boolean
          description: True when the lead driver has confirmed whole-convoy arrival.
        allLegsHandedOver:
          type: boolean
          description: True when the lead driver has confirmed whole-Delivery-Order handover.
        leadPodOtpEnabled:
          type: boolean
    ReturnedGoodsReceiveRequest:
      type: object
      properties:
        notes:
          type: string
          maxLength: 1000
    ReturnedGoodsCountQcRequest:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ReturnedGoodsCountQcItem"
        notes:
          type: string
          maxLength: 1000
    ReturnedGoodsCountQcItem:
      type: object
      required:
        - doItemId
        - productId
        - batchId
        - actualQty
        - qualityPassQty
        - qualityFailQty
      properties:
        doItemId:
          type: integer
          format: int64
        productId:
          type: integer
          format: int64
        batchId:
          type: integer
          format: int64
        actualQty:
          type: number
          minimum: 0
        qualityPassQty:
          type: number
          minimum: 0
        qualityFailQty:
          type: number
          minimum: 0
        qualityFailureReason:
          type: string
          maxLength: 1000
          description: Required when qualityFailQty is greater than zero
    ReturnedGoodsApprovalRequest:
      type: object
      required:
        - decision
      properties:
        decision:
          type: string
          enum:
            - ACCEPT
            - REJECT
        rejectionReason:
          type: string
          maxLength: 1000
          description: Required when decision is REJECT
        notes:
          type: string
          maxLength: 1000
    ReturnedGoodsPutawayPlanRequest:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ReturnedGoodsPutawayPlanItem"
        notes:
          type: string
          maxLength: 1000
    ReturnedGoodsPutawayPlanItem:
      type: object
      required:
        - doItemId
        - batchId
        - destinationLocationId
        - plannedQty
      properties:
        doItemId:
          type: integer
          format: int64
        batchId:
          type: integer
          format: int64
        destinationLocationId:
          type: integer
          format: int64
        plannedQty:
          type: number
          minimum: 0
    ReturnedGoodsPutawayCompleteRequest:
      type: object
      properties:
        notes:
          type: string
          maxLength: 1000
    ReturnedGoodsFlowResponse:
      type: object
      properties:
        doId:
          type: integer
          format: int64
        doNumber:
          type: string
        deliveryOrderStatus:
          type: string
          example: RETURNED
        flowStatus:
          type: string
          enum:
            - COUNT_QC_PENDING
            - COUNT_QC_SUBMITTED
            - QC_REJECTED
            - QC_APPROVED
            - PUTAWAY_PLANNED
            - PUTAWAY_COMPLETED
          example: PUTAWAY_PLANNED
        rejectionReason:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/ReturnedGoodsFlowItemResponse"
    ReturnedGoodsFlowItemResponse:
      type: object
      properties:
        doItemId:
          type: integer
          format: int64
        productId:
          type: integer
          format: int64
        batchId:
          type: integer
          format: int64
        expectedQty:
          type: number
        actualQty:
          type: number
        qualityPassQty:
          type: number
        qualityFailQty:
          type: number
        qualityFailureReason:
          type: string
        destinationLocationId:
          type: integer
          format: int64
        putawayCompletedQty:
          type: number
    TripDriverViewResponse:
      type: object
      properties:
        tripId:
          type: integer
          format: int64
        tripNumber:
          type: string
        status:
          type: string
          example: IN_TRANSIT
        tripType:
          type: string
          enum:
            - DELIVERY
            - TRANSFER
          example: DELIVERY
        tripTypeLabel:
          type: string
          example: Giao dai ly
        transferId:
          type: integer
          format: int64
          nullable: true
          description: Present for TRANSFER rows so the driver app can open the internal transfer detail.
        driverId:
          type: integer
          format: int64
        driverName:
          type: string
        vehicleId:
          type: integer
          format: int64
        vehiclePlate:
          type: string
        plannedStartAt:
          type: string
          format: date-time
        plannedEndAt:
          type: string
          format: date-time
        totalWeightKg:
          type: number
        totalVolumeM3:
          type: number
        deliveryStopCount:
          type: integer
          description: Present for DELIVERY card summaries.
        sourceWarehouseCode:
          type: string
          description: Present for TRANSFER card summaries.
        destinationWarehouseCode:
          type: string
          description: Present for TRANSFER card summaries.
        transferLineCount:
          type: integer
          description: Present for TRANSFER card summaries.
        deliveryOrders:
          type: array
          items:
            $ref: "#/components/schemas/DriverDeliveryOrderResponse"
    DriverDeliveryOrderResponse:
      type: object
      properties:
        doId:
          type: integer
          format: int64
        doNumber:
          type: string
        dealerName:
          type: string
        dealerAddress:
          type: string
        status:
          type: string
          example: IN_TRANSIT
        stopOrder:
          type: integer
        currentAttempt:
          $ref: "#/components/schemas/DeliveryAttemptResponse"
    DeliveryAttemptResponse:
      type: object
      properties:
        deliveryId:
          type: integer
          format: int64
        attemptNumber:
          type: integer
        status:
          type: string
          example: IN_TRANSIT
        goodsImageAvailable:
          type: boolean
        signedDocumentImageAvailable:
          type: boolean
        podTimestamp:
          type: string
          format: date-time
        otpVerifiedAt:
          type: string
          format: date-time
        failureReason:
          type: string
        dispatchedAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
    DeliveryOtpResponse:
      type: object
      properties:
        deliveryId:
          type: integer
          format: int64
        recipientEmail:
          type: string
        status:
          type: string
          enum: [PENDING, ACTIVE, SEND_FAILED, EXPIRED, LOCKED, VERIFIED]
          example: ACTIVE
        expiresAt:
          type: string
          format: date-time
        attemptCount:
          type: integer
    ErrorResponse:
      type: object
      properties:
        code:
          type: string
          examples:
            - DRIVER_NOT_ASSIGNED_TO_TRIP
            - DELIVERY_ATTEMPT_NOT_FOUND
            - DELIVERY_ATTEMPT_NOT_CURRENT
            - DELIVERY_ALREADY_FINALIZED
            - POD_FILE_INVALID
            - POD_EVIDENCE_NOT_FOUND
            - POD_STORAGE_UNAVAILABLE
            - MISSING_POD
            - DEALER_EMAIL_MISSING
            - OTP_NOT_REQUESTED
            - DELIVERY_OTP_INVALID
            - DELIVERY_OTP_EXPIRED
            - OTP_STILL_ACTIVE
            - OTP_MAX_ATTEMPTS_EXCEEDED
            - OTP_RESET_REQUIRED
            - OTP_DELIVERY_FAILED
            - SPLIT_LEAD_DRIVER_REQUIRED
            - SPLIT_DELIVERY_INCOMPLETE
            - SPLIT_LEG_DRIVER_MISMATCH
            - PARTIAL_DELIVERY_NOT_ALLOWED
            - IN_TRANSIT_STOCK_NOT_FOUND
            - INVOICE_ALREADY_EXISTS
            - TRIP_NOT_READY_TO_COMPLETE
            - RETURN_FLOW_STATUS_INVALID
            - RETURN_COUNT_QC_INCOMPLETE
            - RETURN_QC_REJECTION_REASON_REQUIRED
            - RETURN_QC_REWORK_REQUIRED
            - RETURN_QTY_SPLIT_MISMATCH
            - RETURN_QTY_MISMATCH
            - RETURN_QC_ACCEPTANCE_REQUIRED
            - RETURN_PUTAWAY_PLAN_REQUIRED
            - RETURN_PUTAWAY_MISMATCH
            - INVENTORY_VERSION_CONFLICT
        message:
          type: string
