server:
  port: ${PORT:8081}
  servlet:
    context-path: /

spring:
  application:
    name: wms-backend
  jackson:
    deserialization:
      accept_float_as_int: false
      fail_on_unknown_properties: true

  # Database Configuration
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      pool-name: WMSHikariCP
      max-lifetime: 2000000
      connection-timeout: 30000

  # JPA & Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate  # Dùng validate để Flyway quản lý schema, tránh xung đột View
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  # Flyway Database Migration
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    ignore-migration-patterns:
      - "*:ignored"

  # Mail Configuration (Gmail SMTP for OTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: vudinhphuong1210@gmail.com
    password: bmiojdozwksxjetw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:9a4f2c8d3b7a1e5f8c2b4d6e9f1a3c5b7d9e2f4a6b8c0d2e4f6a8b0c2d4e6f8a}
  access-token-expiry: ${JWT_ACCESS_TOKEN_EXPIRY:900}      # 15 minutes
  refresh-token-expiry: ${JWT_REFRESH_TOKEN_EXPIRY:86400}  # 24 hours